# =============================================================================
# PRODUCTION Environment - Docker Compose
# =============================================================================
# Infrastructure for production environment
# Usage: docker-compose -f docker/docker-compose-production.yml up -d
# =============================================================================

version: '3.8'

services:
  audit-mysql-production:
    image: mysql:8.0
    container_name: audit-mysql-production
    ports:
      - "3310:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: quckapp_audit
    volumes:
      - audit_mysql_production_data:/var/lib/mysql
    networks:
      - audit-production-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci --innodb-buffer-pool-size=1G --max-connections=500
    deploy:
      resources:
        limits:
          memory: 4G
        reservations:
          memory: 2G
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3

  audit-elasticsearch-production:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: audit-elasticsearch-production
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=true
      - ELASTIC_PASSWORD=${ELASTICSEARCH_PASSWORD}
      - "ES_JAVA_OPTS=-Xms4g -Xmx4g"
    ports:
      - "9200:9200"
    volumes:
      - audit_elasticsearch_production_data:/usr/share/elasticsearch/data
    networks:
      - audit-production-network
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q 'yellow\\|green'"]
      interval: 30s
      timeout: 10s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 8G
        reservations:
          memory: 4G
      restart_policy:
        condition: on-failure
        delay: 5s
        max_attempts: 3

  audit-zookeeper-production:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: audit-zookeeper-production
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - audit_zookeeper_production_data:/var/lib/zookeeper/data
    networks:
      - audit-production-network
    healthcheck:
      test: echo srvr | nc localhost 2181 || exit 1
      interval: 10s
      timeout: 5s
      retries: 5

  audit-kafka-production:
    image: confluentinc/cp-kafka:7.5.0
    container_name: audit-kafka-production
    ports:
      - "29095:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: audit-zookeeper-production:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://audit-kafka-production:9092,PLAINTEXT_HOST://localhost:29095
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'false'
      KAFKA_LOG_RETENTION_HOURS: 168
      KAFKA_LOG_RETENTION_BYTES: 2147483648
      KAFKA_MESSAGE_MAX_BYTES: 10485760
    volumes:
      - audit_kafka_production_data:/var/lib/kafka/data
    depends_on:
      audit-zookeeper-production:
        condition: service_healthy
    networks:
      - audit-production-network
    healthcheck:
      test: kafka-topics --bootstrap-server localhost:9092 --list || exit 1
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  audit_mysql_production_data:
  audit_elasticsearch_production_data:
  audit_zookeeper_production_data:
  audit_kafka_production_data:

networks:
  audit-production-network:
    driver: bridge
