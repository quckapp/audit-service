# =============================================================================
# UAT3 Environment - Docker Compose
# =============================================================================
# Infrastructure for UAT3 environment
# Usage: docker-compose -f docker/docker-compose-uat3.yml up -d
# =============================================================================

version: '3.8'

services:
  audit-mysql-uat3:
    image: mysql:8.0
    container_name: audit-mysql-uat3
    ports:
      - "3310:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: quckapp_audit
    volumes:
      - audit_mysql_uat3_data:/var/lib/mysql
    networks:
      - audit-uat3-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci

  audit-elasticsearch-uat3:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.11.0
    container_name: audit-elasticsearch-uat3
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms1g -Xmx1g"
    ports:
      - "9200:9200"
    volumes:
      - audit_elasticsearch_uat3_data:/usr/share/elasticsearch/data
    networks:
      - audit-uat3-network
    healthcheck:
      test: ["CMD-SHELL", "curl -s http://localhost:9200/_cluster/health | grep -q 'yellow\\|green'"]
      interval: 30s
      timeout: 10s
      retries: 5

  audit-zookeeper-uat3:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: audit-zookeeper-uat3
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    volumes:
      - audit_zookeeper_uat3_data:/var/lib/zookeeper/data
    networks:
      - audit-uat3-network
    healthcheck:
      test: echo srvr | nc localhost 2181 || exit 1
      interval: 10s
      timeout: 5s
      retries: 5

  audit-kafka-uat3:
    image: confluentinc/cp-kafka:7.5.0
    container_name: audit-kafka-uat3
    ports:
      - "29095:29092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: audit-zookeeper-uat3:2181
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://audit-kafka-uat3:9092,PLAINTEXT_HOST://localhost:29095
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    volumes:
      - audit_kafka_uat3_data:/var/lib/kafka/data
    depends_on:
      audit-zookeeper-uat3:
        condition: service_healthy
    networks:
      - audit-uat3-network
    healthcheck:
      test: kafka-topics --bootstrap-server localhost:9092 --list || exit 1
      interval: 30s
      timeout: 10s
      retries: 5

volumes:
  audit_mysql_uat3_data:
  audit_elasticsearch_uat3_data:
  audit_zookeeper_uat3_data:
  audit_kafka_uat3_data:

networks:
  audit-uat3-network:
    driver: bridge
